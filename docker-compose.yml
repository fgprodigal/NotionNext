version: '3.5'
services:
  notion-next:
    container_name: notion-next
    build: .
    restart: unless-stopped
    networks:
      network:
        ipv4_address: 172.25.0.2

networks:
  network:
    ipam:
      config:
        - subnet: 172.25.0.0/24